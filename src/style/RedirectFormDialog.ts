import { style } from 'typestyle';

export const RedirectFormDialog_usernameInputClass = style({
  marginTop: '10px'
});

export const RedirectFormDialog_ContentClass = style({
  display: 'flex',
  flexDirection: 'column'
});

export const RedirectFormDialog_DropDownListWrapperClass = style({
  position: 'relative',
  marginTop: '10px'
});

export const RedirectFormDialog_WarningClass = style({
  whiteSpace: 'pre-line'
});
